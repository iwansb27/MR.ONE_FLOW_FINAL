# PILIHKAN Backend

Reserved integration workspace for the PILIHKAN traffic/affiliate system.

Architecture target:
B12 frontend -> PILIHKAN API -> Tavily + OpenRouter -> Turso -> B12

This file is intentionally placed in the existing MR.ONE_FLOW_FINAL repository only as a temporary staging location because the connected GitHub tools currently do not expose repository creation. No existing application files are modified.
